# TODO

_The list order below is random. When a feature is implemented, it is removed from the list._

- Add account-level biometry (authenticate when the app enters the foreground)
- Add payment notes feature
- Add switch/add-new account feature
- Add localization support
- Refactor `PaymentArchive.AppState` stream using the Observations API (experiment)
